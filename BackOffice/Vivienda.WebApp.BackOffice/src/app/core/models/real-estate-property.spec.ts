import { RealEstateProperty } from './real-estate-property';

describe('RealEstateProperty', () => {
  it('should create an instance', () => {
    expect(new RealEstateProperty()).toBeTruthy();
  });
});
